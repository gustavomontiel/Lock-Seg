import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guazuseguridad.app',
  appName: 'Guazú',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
